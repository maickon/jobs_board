= simple_form_for(@job, html: { class: "form-horizontal" }) do |f|
	.row
		= f.collection_select :category_id, Category.all, :id, :name, { prompt: "Choose a category" }, {class: "selectpicker", "data-live-search" => "true" }
	= f.input :title, label: "Job Title", input_html: { class: "form-control"}
	= f.input :description, label: "Description", input_html: { class: "form-control", rows:"5"}
	= f.input :company, label: "Your Company", input_html: { class: "form-control"}
	= f.input :url, label: "Link to Job", input_html: { class: "form-control"}
	.row
		= f.button :submit